{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /e/claude && git add -u && (git diff --cached --quiet || (git commit -m \"auto: $(date '+%Y-%m-%d %H:%M:%S')\" && git push origin HEAD)) 2>/dev/null || true",
            "statusMessage": "Auto-committing changes to GitHub..."
          }
        ]
      }
    ]
  }
}
